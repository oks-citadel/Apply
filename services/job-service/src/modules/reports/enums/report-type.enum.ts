export enum ReportType {
  SPAM = 'spam',
  EXPIRED = 'expired',
  MISLEADING = 'misleading',
  DUPLICATE = 'duplicate',
  INAPPROPRIATE = 'inappropriate',
  OTHER = 'other',
}

export enum ReportStatus {
  PENDING = 'pending',
  REVIEWED = 'reviewed',
  RESOLVED = 'resolved',
  DISMISSED = 'dismissed',
}
