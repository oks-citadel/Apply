# Environment
NODE_ENV=development

# Service Configuration
PORT=8007
SERVICE_VERSION=1.0.0

# Frontend URL
# Production: Use production URL
# Local Dev: Use localhost
FRONTEND_URL=https://applyforus.com

# Local Development (Override in .env.local)
# FRONTEND_URL=http://localhost:3000

# Database Configuration - Azure PostgreSQL
# Production: Azure Database for PostgreSQL
# Local Dev: Use localhost or Docker Compose
DB_HOST=applyforus-postgres.postgres.database.azure.com
DB_PORT=5432
DB_USERNAME=applyforusadmin@applyforus-postgres
DB_PASSWORD=<your-db-password-here>
DB_DATABASE=applyforus
DB_SSL=true
DB_SSL_CA_CERT=

# Local Development (Override in .env.local)
# DB_HOST=localhost
# DB_PORT=5432
# DB_USERNAME=postgres
# DB_PASSWORD=postgres
# DB_SSL=false

# Redis Configuration - Azure Cache for Redis
# Production: Azure Cache for Redis with SSL/TLS
# Local Dev: Use localhost or Docker Compose
# Used for Bull queues
REDIS_HOST=applyforus-redis.redis.cache.windows.net
REDIS_PORT=6380
REDIS_PASSWORD=<your-redis-access-key-here>
REDIS_DB=0
REDIS_TLS=true
REDIS_SSL=true

# Local Development (Override in .env.local)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_TLS=false

# SMTP Configuration (MailHog for development)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=
SMTP_PASSWORD=

# Email Configuration
EMAIL_FROM=noreply@jobapply.com
EMAIL_FROM_NAME=Job Apply Platform

# Logging
LOG_LEVEL=info

# Azure Application Insights (Optional)
APPLICATIONINSIGHTS_CONNECTION_STRING=
APPLICATIONINSIGHTS_INSTRUMENTATION_KEY=

# Monitoring (Optional)
ENABLE_METRICS=true
METRICS_PORT=9090

# ============================================
# Push Notification Configuration
# ============================================

# Firebase Cloud Messaging (FCM) Configuration
# For Android and Web Push Notifications
# Obtain from Firebase Console > Project Settings > Service Accounts
# Set as JSON string or file path
FCM_SERVICE_ACCOUNT='{"type":"service_account","project_id":"your-project-id",...}'
# Alternatively, use file path for local development:
# FCM_SERVICE_ACCOUNT_PATH=/path/to/firebase-service-account.json

# Apple Push Notification Service (APNs) Configuration
# For iOS Push Notifications
# Obtain from Apple Developer Portal > Certificates, Identifiers & Profiles

# APNs Key ID (10 character string from Apple Developer Portal)
APNS_KEY_ID=YOUR_KEY_ID

# APNs Team ID (10 character string from Apple Developer Portal)
APNS_TEAM_ID=YOUR_TEAM_ID

# APNs Private Key (contents of the .p8 file, replace newlines with \n)
APNS_KEY='-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_HERE\n-----END PRIVATE KEY-----'

# APNs Environment (true for production, false for development/sandbox)
APNS_PRODUCTION=false
