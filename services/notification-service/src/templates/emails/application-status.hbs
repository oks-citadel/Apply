<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <style>
      body {
        font-family: Arial, sans-serif;
        line-height: 1.6;
        color: #333;
        margin: 0;
        padding: 0;
        background-color: #f4f4f4;
      }
      .container {
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
        background-color: #ffffff;
      }
      .header {
        background: {{statusColor}};
        color: white;
        padding: 20px;
        text-align: center;
        border-radius: 5px 5px 0 0;
      }
      .content {
        background: #f9f9f9;
        padding: 30px;
        border-radius: 0 0 5px 5px;
      }
      .job-info {
        background: white;
        padding: 20px;
        border-radius: 5px;
        margin: 20px 0;
        border-left: 4px solid {{statusColor}};
      }
      .job-title {
        font-size: 20px;
        font-weight: bold;
        color: #333;
        margin: 0 0 10px 0;
      }
      .company-name {
        color: #666;
        font-size: 16px;
        margin: 5px 0;
      }
      .status-badge {
        display: inline-block;
        padding: 8px 20px;
        background: {{statusColor}};
        color: white;
        border-radius: 20px;
        font-size: 14px;
        font-weight: bold;
        text-transform: uppercase;
        margin: 10px 0;
      }
      .button {
        display: inline-block;
        padding: 12px 30px;
        background: {{statusColor}};
        color: white !important;
        text-decoration: none;
        border-radius: 5px;
        margin: 20px 0;
        font-weight: bold;
      }
      .message-box {
        background: #E0E7FF;
        border-left: 4px solid #4F46E5;
        padding: 15px;
        margin: 20px 0;
        border-radius: 0 5px 5px 0;
      }
      .footer {
        text-align: center;
        margin-top: 20px;
        font-size: 12px;
        color: #666;
        padding: 20px;
        border-top: 1px solid #e0e0e0;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <div class="header">
        <h1>Application Status Update</h1>
      </div>
      <div class="content">
        <p>Hello {{name}},</p>

        <p>Your application status has been updated:</p>

        <div class="job-info">
          <div class="job-title">{{jobTitle}}</div>
          <div class="company-name">{{companyName}}</div>
          {{#if location}}
          <div style="color: #666; margin: 5px 0;">📍 {{location}}</div>
          {{/if}}
          <div style="margin-top: 15px;">
            <span class="status-badge">{{status}}</span>
          </div>
        </div>

        {{#if message}}
        <div class="message-box">
          <strong>Message from employer:</strong>
          <p style="margin: 10px 0 0 0;">{{message}}</p>
        </div>
        {{/if}}

        {{#if nextSteps}}
        <div style="background: white; padding: 15px; border-radius: 5px; margin: 20px 0;">
          <strong>Next Steps:</strong>
          <p style="margin: 10px 0 0 0;">{{nextSteps}}</p>
        </div>
        {{/if}}

        <div style="text-align: center;">
          <a href="{{applicationUrl}}" class="button">View Application Details</a>
        </div>

        <p>Keep track of all your applications in your dashboard and stay updated on their progress.</p>

        <p>Best of luck!<br>The Job Apply Platform Team</p>
      </div>
      <div class="footer">
        <p>&copy; {{year}} Job Apply Platform. All rights reserved.</p>
        <p>Manage your notification preferences in your account settings.</p>
      </div>
    </div>
  </body>
</html>
